import { Callout } from 'nextra/components'

# 循环

**循环**重复代码块。与其把同样的代码复制粘贴多遍，不如写一次，告诉 Python 运行多少次。

Python 有两种循环：

- **`for` 循环** — 对序列中的每个项目运行一次（列表、数字范围等）
- **`while` 循环** — 只要条件为真就继续运行

<Callout type="info" title="词汇 — 迭代">
循环每运行一次其代码块就称为一次**迭代**。
</Callout>

使用下面的链接了解每种循环的用法。
